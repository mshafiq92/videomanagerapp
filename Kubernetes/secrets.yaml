# Kubernetes Secrets for Video Dashboard Application
apiVersion: v1
kind: Secret
metadata:
  name: video-dashboard-secrets
  namespace: default
type: Opaque
stringData:
  # MongoDB Atlas connection string (plain text - Kubernetes will encode automatically)
  mongo-uri: "mongodb+srv://youtubepy:youtubepy12@cluster0.ayornwa.mongodb.net/?retryWrites=true&w=majority"

  # MongoDB credentials for local MongoDB (if using local instead of Atlas)
  mongo-username: "admin"
  mongo-password: "password123"
